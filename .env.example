# pick one provider; MODEL decides which is used (default gpt-4o-mini)
OPENAI_API_KEY=sk-...
# MODEL=claude-opus-5 with one of:
# ANTHROPIC_API_KEY=sk-ant-api...
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat...   # from `claude setup-token`, local dev only
